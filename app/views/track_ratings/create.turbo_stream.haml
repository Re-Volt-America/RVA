= turbo_stream.replace track_rating_summary_dom_id(@track) do
  = render "track_ratings/overview", track: @track

= turbo_stream.replace "track-rating-form" do
  = render "track_ratings/form", track: @track, track_rating: @track_rating, saved: saved
