- if track.track_ratings.any?
  .track-rating-admin-breakdown.card.mt-4
    .card-header.track-rating-admin-header
      %h4.mb-0= t("rva.tracks.rating.admin.title")
      %span.badge.badge-light= t("rva.tracks.rating.votes", count: track.rating_count)

    .card-body.p-0
      .table-responsive
        %table.table.table-hover.table-sm.mb-0
          %thead
            %tr
              %th= t("rva.tracks.rating.admin.columns.user")
              %th= t("rva.tracks.rating.fields.gameplay")
              %th= t("rva.tracks.rating.fields.visuals")
              %th= t("rva.tracks.rating.fields.audio")
              %th= t("rva.tracks.rating.fields.fun")
              %th.text-right= t("rva.tracks.rating.admin.columns.overall")
          %tbody
            - track.track_ratings.each do |rating|
              %tr
                %td.align-middle
                  = link_to user_path(rating.user), :class => "track-rating-admin-user d-flex align-items-center" do
                    - if rating.user.profile&.profile_picture_data.present?
                      = image_tag rating.user.profile.profile_picture_url, class: "rating-admin-avatar mr-2"
                    - else
                      = image_tag "no_profile_picture.png", class: "rating-admin-avatar mr-2"
                    = rating.user.username
                %td.align-middle
                  .rating-pill
                    - (1..5).each do |i|
                      %i.fa{ :class => (i <= rating.gameplay_rating ? "fa-star" : "fa-star-o") }
                    %span.rating-pill-value= rating.gameplay_rating
                %td.align-middle
                  .rating-pill
                    - (1..5).each do |i|
                      %i.fa{ :class => (i <= rating.visuals_rating ? "fa-star" : "fa-star-o") }
                    %span.rating-pill-value= rating.visuals_rating
                %td.align-middle
                  .rating-pill
                    - (1..5).each do |i|
                      %i.fa{ :class => (i <= rating.audio_rating ? "fa-star" : "fa-star-o") }
                    %span.rating-pill-value= rating.audio_rating
                %td.align-middle
                  .rating-pill
                    - (1..5).each do |i|
                      %i.fa{ :class => (i <= rating.fun_rating ? "fa-star" : "fa-star-o") }
                    %span.rating-pill-value= rating.fun_rating
                %td.align-middle.text-right
                  %span.rating-overall-badge
                    = number_with_precision(rating.average_score, precision: 2)
