#ranking
  .container
    %h3.nissan-title.mb-4
      = link_to (image_tag "rva-logo-dark.png", :height => "40", :width => "40", :style => "border-radius: 50px;"), "/"
      = "/"
      = "#{link_to @ranking.season.name, @ranking.season} /"
      = "#{link_to "Ranking #{@ranking.number}", @ranking}"

    %p#notice= notice
    %table.table.table-fluid
      %thead
        %tr
          %th Racer
          %th PP
          %th PA
          %th MP
          %th PO
          %th CC
          %th FJ
      %tbody
        %tr
          %td
            YUN
          %td
            3.040
          %td
            2033
          %td
            0.967
          %td
            64.65
          %td
            174
          %td
            9

    %h5.border-bottom.border-gray.pb-2.mb-0.nissan-title
      = "Sessions"

    - @ranking.sessions.each do |session|
      %a.media.text-muted.pt-3.border-bottom.border-gray{:href => session_path(session) }
        .session-number{:style => "background-color: #{session_color_hex(session)}"}
          .text-center
            %span.align-middle
              = session.number
        .media-body.pb-3.mb-0.small.lh-125
          .d-flex.justify-content-between.align-items-center.w-100
            %strong.text-gray-dark
              = "#{session_category_name(session)} Races"
            .session-date
              = pretty_datetime(session.date)
          %span.d-block.session-host
            = "Hosted by #{session.host}"
