- content_for :title, t("rva.cars.meta.classes.advanced.page-title")
- meta :title, t("rva.cars.meta.classes.advanced.title", org: ORG::NAME_SHORT)
- meta :description, t("rva.cars.meta.classes.advanced.description")
- meta :image, "https://rva.lat/assets/img/og/advanced.png"

#car-cards-display
  %h2
    = "#{t('rva.cars.title')} - #{t("classes.advanced")}"

  = render :partial => "application/subnav", :locals => {:season => current_season, :category => SYS::CATEGORY::ADVANCED}

  .sticky-top.search-bar
    .row
      .col-xl-11.col-lg-10.col-md-10.col-xs-9.col-8.sticky-top
        %input#input.form-control{placeholder: t("misc.placeholder.search"), type: "text"}/
      .col-xl-1.col-lg-2.col-md-2.col-xs-3.col-4
        = render :partial => "car_category_selector"

  - if @cars.none?
    %h5.text-center
      = t("rva.cars.shared.no-cars")
  - else
    .row
      - @cars.each do |car|
        = render :partial => "car_card", :locals => { :car => car }
