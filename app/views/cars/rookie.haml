- content_for :title, "Re-Volt America - #{t("classes.rookie")} Cars"
- meta :title, t("classes.rookie")
- meta :description, "Cars of the Rookie class. These are the cars from the Rookie class required to race online on RVA!"
- meta :image, "https://rva.lat/assets/img/og/rookie.png"

#car-cards-display
  %h2
    = "#{t('rva.cars.title')} - #{t("classes.rookie")}"

  = render :partial => "application/subnav", :locals => {:category => SYS::CATEGORY::ROOKIE}

  .sticky-top.search-bar
    .row
      .col-xl-11.col-lg-10.col-md-10.col-xs-9.col-8.sticky-top
        %input#input.form-control{placeholder: "Search...", type: "text"}/
      .col-xl-1.col-lg-2.col-md-2.col-xs-3.col-4
        = render :partial => "car_category_selector"

  .row
    - @cars.each do |car|
      = render :partial => "car_card", :locals => { :car => car }
